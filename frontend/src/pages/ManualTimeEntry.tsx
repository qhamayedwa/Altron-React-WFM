import PlaceholderPage from '../components/PlaceholderPage';

export default function ManualTimeEntry() {
  return <PlaceholderPage title="Manual Time Entry" description="Manually create or adjust time entries." />;
}
