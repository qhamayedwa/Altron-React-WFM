import PlaceholderPage from '../components/PlaceholderPage';

export default function CreateCompany() {
  return <PlaceholderPage title="Create Company" description="Create new company entities." />;
}
